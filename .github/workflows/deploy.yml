name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: be-runner
    steps:
      - uses: actions/checkout@v3
      - name: Deploy with Docker Compose
        run: |
          docker-compose down
          docker-compose up -d
